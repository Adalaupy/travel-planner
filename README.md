# Travel Planner (local-first)

Minimal scaffold for the Travel Planner app.

Run (after installing deps):

```powershell
npm install
npm run dev
```

Set `NEXT_PUBLIC_IS_DEVELOP=true` in `.env.local` to bypass Clerk in development.
